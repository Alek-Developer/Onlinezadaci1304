<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.gsixacademy.onlinezadaci1304">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".movies.PopularMoviesActivity" />
        <activity android:name=".CarActivity" />
        <activity android:name=".UserSignedInActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RecycleViewActivity" />
        <activity android:name=".TodoListActivity" />
        <activity android:name=".RelativeLayoutActivity" />
        <activity android:name=".CalculatorActivity" />
    </application>

</manifest>